# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
jspm_packages/

# Frontend SPA source — not shipped with the app; only the built assets under
# nesscale_sign/public/frontend (and the generated www html) are committed.
# Leading slash anchors this to the repo-root source dir ONLY, so it does NOT
# also match nesscale_sign/public/frontend (the build output, which IS shipped).
/frontend/

# Sourcemaps are dev-only — never commit them (we only ship the runtime build).
*.map

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*
